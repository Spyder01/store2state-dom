{
  "name": "store2state-dom",
  "version": "0.0.1",
  "description": "An extension of the `store2state` library that provides a straightforward way to bind application state changes to DOM elements in vanilla JavaScript",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"no test specified\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "store2state": "^0.1.0"
  }
}
